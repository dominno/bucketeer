node_modules/
config/profiles.json
# iDrive E2 credential files (contain live secrets) — never commit
*Access-Keys*.txt
.cred_file
.env
*.log
.DS_Store
test-results/
playwright-report/
/tmp_test_*
# Build OUTPUT (installers, packaged .app/.exe) — regenerated by `npm run app:*`. Never commit (615 MB+).
dist/
# NOTE: build/ is build SOURCE (app icon read by electron-builder) and is intentionally tracked.
